// Redis connection config (Stub)
// If you plan to implement Redis caching later, configure it here.
module.exports = {
  client: null,
  connect: async () => {
    // console.log('Redis stub: redis client not configured');
  }
};
